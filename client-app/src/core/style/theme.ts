import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
      primary: {
        main: '#673ab7',
        light: '#8561c5',
      },
      secondary: {
        main: '#8561c5',
      },
      info: {
        main: '#ffffff'
      }
    },
  });

  export default theme;